iab mee charan
iab mal charancuz008gmail.com
set nocp "i strongly discourage it

set guioptions+=M
let g:did_install_default_menus = 1
let g:loaded_netrw       = 1
let g:loaded_netrwPlugin = 1
let g:loaded_vimball      = 1
let g:loaded_vimballPlugin= 1

filetype plugin indent on
syntax on

let mapleader=' '

packadd comment "vims default
packadd nohlsearch "performs noh aut

colo quiet
set autoindent
set autoread
set belloff=all
set colorcolumn+=80
set cursorline
set cursorlineopt=number "just hi the number
set encoding=utf-8 "Self explanatory
set fillchars+=vert:│,fold:·,foldsep:│,eob:\  |"make seperator better exp:removing eob cuz of linenr
set foldcolumn=0 "signcoloumn is better
set hidden " A buffer becomes hidden when it is abandoned
set hlsearch " Highlight search results
set ignorecase " Ignore case when searching
set incsearch " Makes search act like search in modern browsers
set laststatus=2 " always show statusline
set lazyredraw " Don't redraw while executing macros (good performance config)
set noexpandtab
set nojoinspaces "J doesnt join spaces
set noswapfile
set nowrap "noWrap lines
set number " show line numbers
set path+=** "make the find to search through dirs and subdirs
set relativenumber " show relativeline numbers
set shiftwidth=4
set signcolumn=yes "no
set smartcase
set smartindent
set smarttab
set softtabstop-=1"inherit from shiftwidth
set tabstop=4
set termguicolors
set title
set ttimeoutlen=50
set whichwrap+=<,>,h,l "makes cursor move from one to another at ends

if has("win64")
	if has("directx")
		set renderoptions=type:directx
	endif
endif

nnoremap <Esc><Esc> <Cmd>nohlsearch<CR>

nnoremap <leader>w <Cmd>update<CR>
nnoremap <leader>bs <Cmd>ls<CR>:b<Space>
nnoremap <leader>bd <Cmd>bd<CR>
nnoremap <leader>te :tabe<Space>

nnoremap <leader>y :%y+<CR> |"system clipboard
xnoremap <leader>y "+y  |"system clipboard for all visual modes

tnoremap >   |"terminal mode

"emacs style for :
cnoremap <C-A>		<Home>
cnoremap <C-B>		<Left>
cnoremap <C-D>		<Del> "kinda good defautl
" cnoremap <C-F>		<Right> "cant map bcuz of quickfiz

"zed
function! ToggleLineNumbers()
	if &number
		set nonumber norelativenumber
	else
		set number relativenumber
	endif
endfunction

nnoremap <silent> <leader>; :call ToggleLineNumbers()<CR>

" markdown
let g:markdown_fenced_languages = ['cpp']

" exp
lua << EOF
EOF

source <sfile>:h/plugit.vim
